services:
  - type: web
    name: chatbot-militar
    runtime: node
    rootDir: chatbot-militar/backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
